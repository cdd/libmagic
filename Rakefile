require 'rubygems'
require 'hoe'
$:.unshift('./lib')
require './lib/libmagic.rb'

Hoe.spec('libmagic') do |p|
  # p.rubyforge_name = 'libmagicx' # if different than lowercase project name
  p.developer('Moses Hohman', 'moses@moseshohman.com')
end
